# =========================================
## improve command: cd -> cd + ls
cdls ()
{
  \cd "$@" && pwd && tree -L 1 ./
}

alias cd="cdls"

# ==========================================

export EDITOR=vim # exclude evil editor "NANO"
